1. 전체 구조 요약

project/
│
├── backend/          ← Django 프로젝트
│   ├── Dockerfile
│   └── ...
│
├── frontend/         ← React 프로젝트
│   ├── Dockerfile
│   └── ...
│
├── nginx/            ← Nginx 설정 (선택)
│   └── default.conf
│
├── docker-compose.yml
└── .env              ← 환경 변수

2.react 프로젝트
+ Dockerfile 만들기
+ .dockerignore 만들기
+ ngnix 폴더 생성후 default.conf 작성
+ pip freeze > requirements.txt 명령어를 치고 requirements.txt를 확인
+ 서버쪽에 Dockerfile 작성 


